# Install as /etc/sudoers.d/go-irl-channel-workerctl with owner root:root and mode 0440.
# The root-owned helper validates its own bounded action allowlist and exact release SHA.
goirl-runner ALL=(root) NOPASSWD: /usr/local/sbin/go-irl-channel-workerctl *
