# Install as /etc/sudoers.d/go-irl-stage-publish with owner root:root and mode 0440.
# The root-owned helper validates a fixed action allowlist, exact release SHA, numeric run id and fixed publish paths.
goirl-runner ALL=(root) NOPASSWD: /usr/local/sbin/go-irl-stage-publish *
