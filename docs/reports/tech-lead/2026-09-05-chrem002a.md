# ChRem002A — Telegram city activity cards

Status: Partial
Role: Tech Lead
Date: 2026-09-05
Repository: vitvolny26-art/Go-IRL-1.1
Base branch: main
Parent main: a8269299f359b84b944de0d0ad43ceeb5addc11c
Deploy target: none
PR: #1123 merged
Merge: performed
VPS deploy: performed for a8269299f359b84b944de0d0ad43ceeb5addc11c
Supabase Edge deploy: not performed for this follow-up

## Bounded product contract

- City Activity card: immutable photo, editable Activity caption, shared Details + Participate buttons, no physical pin.
- Prepared/personal Telegram share: immutable photo + buttons, no caption text below the photo.
- Kharkiv public Activities publish to the canonical Kharkiv Telegram community.
- Direct `join:<activityId>` callback reuses the existing canonical membership path.

## Follow-up runtime evidence

After VPS deploy of merged PR #1123, production `telegramEventSupergroup` was read back as ACTIVE version 42. Its deployed `postEventCallback.ts` did not contain the new `handleActivityJoinCallback` wrapper from GitHub main, so the Participate callback remained unavailable in the production Telegram webhook runtime.

The user supplied runtime evidence also showed:
- prepared/personal event share rendered duplicate caption text below the image;
- the Kharkiv public Activity was not auto-posted to the Kharkiv city group.

## Sources inspected for this follow-up

- Fresh GitHub `main` at `a8269299f359b84b944de0d0ad43ceeb5addc11c`.
- Production Supabase project `tygfsvjkznypilfyyvdc`, Edge Function `telegramEventSupergroup@v42`.
- `api/telegram/prepared-share.ts`.
- `api/_shared/telegram-city-publication-core.ts`.
- `src/config/cities.ts`.
- `src/telegramShareJoinContract.test.ts`.
- Production data for the observed `Volejbal` Activity `7968768e-d27b-462d-8c4c-222f7300e0ef`, city_id `kharkiv`.

## Verified configuration

Canonical Kharkiv city configuration already exists in `src/config/cities.ts`:
- city id: `kharkiv`
- Telegram chat id: `-1003919911341`
- Telegram URL: `https://t.me/GoIRL_Kharkiv`

The server-side city publication map did not include `kharkiv`; this caused `resolveCityTelegramChatId("kharkiv")` to return null and publication to skip the city.

## Corrective implementation

This commit is intentionally bounded to three concerns:

1. Prepared/personal event share builds the existing Telegram event card, then overrides only `caption` to `""` before `savePreparedInlineMessage`. The common event-card builder remains unchanged, so city publication retains editable caption text.
2. Server city publication map adds canonical Kharkiv chat id `-1003919911341`.
3. Join callback source in GitHub main is not rewritten because it is already present. The remaining join problem is deployment drift: production `telegramEventSupergroup@v42` must later be deployed from the approved exact source under a separate production mutation gate.

Contract tests cover prepared personal captionless behavior, canonical Kharkiv resolution, and continued presence of the direct join callback boundary.

## Checks / release state

No branch ref is moved by this commit-only workflow.
No push has been performed.
No new CI exists yet for this corrective commit.
No PR, merge, VPS deploy, or Supabase Edge deploy is performed by this follow-up commit creation.

## Known gaps / blockers

- Exact-head pnpm/CI evidence requires a later approved push/ref update.
- Production Telegram join remains RED until `telegramEventSupergroup` is deployed from the approved exact source and reread.
- Runtime E2E still requires one personal share check, one Kharkiv publication check, and one Participate callback check after deploys.
- Existing unrelated city mappings were preserved; this slice does not refactor city configuration architecture.
- `telegramMediaOrigin` inherited Vercel routing remains outside this bounded follow-up.

## Evidence ledger

| Claim | Evidence | Scope |
| --- | --- | --- |
| Current base is merged main `a826929` | fresh GitHub main readback | Repository base |
| Prepared share currently uses the common event card directly | `api/telegram/prepared-share.ts` | Personal Telegram share |
| Kharkiv canonical chat id is `-1003919911341` | `src/config/cities.ts` | City configuration |
| Server city publisher lacked Kharkiv | `api/_shared/telegram-city-publication-core.ts` before corrective commit | City publication |
| GitHub main already contains `handleActivityJoinCallback` | `supabase/functions/telegramEventSupergroup/postEventCallback.ts` | Join source |
| Production Edge runtime is stale at v42 | Supabase Edge Function readback | Production Telegram runtime |
| No release mutation is included in commit creation | commit-object-only workflow | Release state |

## Next safe action

Read back the corrective commit object and verify `main` is unchanged. Push/ref update, CI, PR, merge, VPS deploy and Supabase Edge deploy remain separate explicit approval gates.
